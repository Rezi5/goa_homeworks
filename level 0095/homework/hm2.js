// Homework2

let Min = (a,b) =>{
    return Math.min(a,b) 
}

let Max = (c,d) =>{
    return Math.max(c,d) 
}

let average = (e,v) =>{
    return (e + v) / 2
}

let Combine = {Min,Max,average}

export default Combine